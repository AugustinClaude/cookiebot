const ytdl = require("ytdl-core");

module.exports.run = async (bot, message, args) => {};

module.exports.conf = {
  enabled: true,
  guildOnly: false,
  aliases: [],
  permLevel: 0
};

module.exports.help = {
  name: "pause"
};
